
; HELLO1.ASM -  ?  ? ?? .?. N1
;                ? "? "
; *****************************************************************
; ?: ?? ?  ?  ? ??
;             ??   ??  "? ?"
;             ( 09 ? 21h), :
;              - ?? ?  ? ? ?,
;                ??  "$";
;              - ?   ? ah  ??=09h,
;                  ? dx -  ??    ?
;                ?;
;              - ? ?  ax     ? 
;                ??.
; ******************************************************************

   DOSSEG                                      ;  ??  
   .MODEL  SMALL                               ;  -SMALL()
   .STACK  100h                                ; ?  ? 256 
   .DATA                                       ; ? ? 
Greeting  LABEL  BYTE                          ;   ??
   DB '‚ α ―ΰ¨Ά¥βαβΆγ¥β αβ.£ΰ.0383 -  ΰδ¥­®Ά ‚..',13,10,'$'
   .CODE                                ; ? ? 
   mov  ax, @data                        ; ?  DS  ?
   mov  ds, ax                           ; ? 
   mov  dx, OFFSET Greeting              ; ?  dx ??
                                        ;  ? ??
DisplayGreeting:
   mov  ah, 9                            ; # ??   ?
   int  21h                             ; ?  ?  ??
   mov  ah, 4ch                          ; # ??  ? ??
   int  21h                             ; ? ??  ?  
   END
